# Server Configuration
PORT=5001

# Database Configuration (PostgreSQL)
# Example: postgresql://username:password@host:port/database
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=seniorservice_db
# Or, if you prefer a single connection string:
# DATABASE_URL=postgresql://your_db_user:your_db_password@localhost:5432/seniorservice_db

# API Keys (if any, add later)
# SOME_API_KEY=your_api_key_here

# JWT Secret (if using JWT for authentication, add later)
# JWT_SECRET=your_super_secret_jwt_key